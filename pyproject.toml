[tool.isort]
profile = "black"

[tool.pylint.'MESSAGES CONTROL']
disable = "missing-module-docstring"

[tool.pytest.ini_options]
addopts = "--cov=palign --cov-branch --cov-report=html --cov-report=term-missing:skip-covered --cov-report=xml --no-cov-on-fail"
log_cli = 1
testpaths = "tests"
